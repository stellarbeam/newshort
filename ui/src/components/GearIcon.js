import React from 'react';
import {View} from 'react-native';
import EvilIcon from 'react-native-vector-icons/EvilIcons';

const GearIcon = () => {
  return (
    <View>
      <EvilIcon name="gear" size={40} color="white" />
    </View>
  );
};

export default GearIcon;
